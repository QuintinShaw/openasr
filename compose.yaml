services:
  openasr:
    build:
      context: .
    image: openasr:local
    ports:
      - "8080:8080"
    environment:
      OPENASR_HOME: /data
    volumes:
      - openasr-data:/data
    command: ["serve", "--addr", "0.0.0.0:8080"]

volumes:
  openasr-data:
