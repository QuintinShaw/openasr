# Per-model card prose for openasr/sensevoice-small. Distilled (paraphrased,
# not copied) from the upstream FunAudioLLM/SenseVoiceSmall model card. Claims
# are kept to what the upstream card states; re-verify against the upstream
# card when the upstream weights change. Redistribution is under the FunASR
# Model License v1.1, which carries an attribution obligation -- the
# acknowledgement below names the upstream authors and license on purpose.

tagline = "Fast multilingual speech recognition from FunAudioLLM — non-autoregressive SenseVoice, tuned for Chinese, Cantonese, English, Japanese and Korean"
tagline_zh = "FunAudioLLM 出品的高速多语言语音识别 — 非自回归 SenseVoice,面向中文、粤语、英语、日语、韩语优化"

highlights = [
  "🌏 **Multilingual, zh-first** — high-precision Mandarin, Cantonese, English, Japanese and Korean with automatic language detection",
  "⚡ **Non-autoregressive speed** — an end-to-end architecture the upstream clocks at about 70 ms for 10 seconds of audio, 15 times faster than Whisper-Large",
  "🀄 **Chinese benchmark strength** — trained on over 400,000 hours of speech; the upstream reports better Chinese and Cantonese accuracy than Whisper on AISHELL and WenetSpeech",
  "🪶 **Compact and local** — a small checkpoint that transcribes fully offline, from a 130 MB q4_k build up to full-fidelity fp16",
  "🦀 **Native in OpenASR** — `.oasr` packs run with no Python at inference, engineered for peak performance on CPU & GPU",
]

highlights_zh = [
  "🌏 **多语言、中文优先** — 高精度普通话、粤语、英语、日语、韩语,并支持自动语种识别",
  "⚡ **非自回归高速推理** — 端到端架构,上游实测 10 秒音频约 70 ms,比 Whisper-Large 快 15 倍",
  "🀄 **中文基准表现突出** — 超过 400,000 小时语音训练;上游报告在 AISHELL 与 WenetSpeech 上中文、粤语准确率优于 Whisper",
  "🪶 **小巧本地化** — small 检查点完全离线转写,从 130 MB 的 q4_k 到全保真 fp16",
  "🦀 **OpenASR 原生运行** — `.oasr` 包推理阶段无需 Python,针对 CPU 与 GPU 的极致性能优化",
]

intro = """
SenseVoice Small is the compact member of **SenseVoice**, the speech understanding model family
open-sourced by **FunAudioLLM** (Alibaba). Trained on more than **400,000 hours** of speech, it
delivers high-precision transcription with automatic language detection for **Mandarin Chinese,
Cantonese, English, Japanese and Korean**, and the upstream card reports Chinese and Cantonese
accuracy ahead of Whisper on open benchmarks such as AISHELL and WenetSpeech. Its
**non-autoregressive end-to-end** architecture makes inference exceptionally fast — the upstream
team clocks about 70 ms for 10 seconds of audio, 15x faster than Whisper-Large. The upstream model
also carries speech emotion recognition and audio event detection; the OpenASR packs currently
surface plain transcription only (emotion/event tags are not yet exposed). This OpenASR repo
repackages the original weights as `.oasr` packs that run natively in the OpenASR runtime — no
Python at inference time. The **fp16** build is the recommended default for maximum fidelity;
**q8_0** halves the footprint at near-reference quality and **q4_k** suits tight-memory devices.
"""

intro_zh = """
SenseVoice Small 是 **FunAudioLLM**(阿里巴巴)开源的 SenseVoice 语音理解模型家族中的轻量成员。
它以超过 400,000 小时语音训练,为**普通话、粤语、英语、日语、韩语**提供高精度转写与自动语种识别,
上游模型卡报告其中文、粤语准确率在 AISHELL、WenetSpeech 等公开基准上优于 Whisper。
**非自回归端到端**架构带来极低推理延迟——上游实测 10 秒音频约 70 ms,比 Whisper-Large 快 15 倍。
上游模型还具备情感识别与声学事件检测能力;OpenASR 包目前只输出纯转写文本(暂不暴露情感/事件标签)。

这个仓库提供 OpenASR `.oasr` 格式包。OpenASR 会在本地完成加载、校验和推理,不会把音频上传到云端。
`fp16` 是推荐默认版本(最高保真);`q8_0` 在接近参考质量的同时将体积减半;`q4_k` 适合内存更紧的设备。
"""

acknowledgement = """
This pack is a redistribution of **SenseVoice Small**, created and open-sourced by the
**FunAudioLLM team at Alibaba**
([FunAudioLLM/SenseVoiceSmall](https://huggingface.co/FunAudioLLM/SenseVoiceSmall)), built on the
**FunASR** open-source speech toolkit from Alibaba's ModelScope community. All credit for the
original architecture, training, and weights belongs to the FunAudioLLM and FunASR teams; the
license is inherited from and identical to the upstream model — the
**[FunASR Model License v1.1](https://github.com/modelscope/FunASR/blob/main/MODEL_LICENSE)**,
which permits commercial use and requires this attribution. Thank you to FunAudioLLM, the FunASR
team, and Alibaba for releasing their work openly. OpenASR only performs format conversion,
quantization, runtime verification, and local-inference adaptation.
"""

acknowledgement_zh = """
本模型包重新分发自 **阿里巴巴 FunAudioLLM 团队** 开源的 SenseVoice Small
([FunAudioLLM/SenseVoiceSmall](https://huggingface.co/FunAudioLLM/SenseVoiceSmall)),
构建于阿里巴巴 ModelScope 社区的 **FunASR** 开源语音工具箱之上。原始架构、训练和权重归
FunAudioLLM 与 FunASR 团队所有;许可证继承上游
**[FunASR Model License v1.1](https://github.com/modelscope/FunASR/blob/main/MODEL_LICENSE)**,
该许可允许商业使用并要求署名(本致谢即履行该义务)。感谢 FunAudioLLM、FunASR 团队与阿里巴巴的
开源工作。OpenASR 仅进行格式转换、量化、运行时校验和本地推理适配。
"""

[prose_locales."zh-CN"]
tagline = "FunAudioLLM 出品的高速多语言语音识别 — 非自回归 SenseVoice,面向中文、粤语、英语、日语、韩语优化"

highlights = [
  "🌏 **多语言、中文优先** — 高精度普通话、粤语、英语、日语、韩语,并支持自动语种识别",
  "⚡ **非自回归高速推理** — 端到端架构,上游实测 10 秒音频约 70 ms,比 Whisper-Large 快 15 倍",
  "🀄 **中文基准表现突出** — 超过 400,000 小时语音训练;上游报告在 AISHELL 与 WenetSpeech 上中文、粤语准确率优于 Whisper",
  "🪶 **小巧本地化** — small 检查点完全离线转写,从 130 MB 的 q4_k 到全保真 fp16",
  "🦀 **OpenASR 原生运行** — `.oasr` 包推理阶段无需 Python,针对 CPU 与 GPU 的极致性能优化",
]

source_sha256 = "9d740e66df2b33cb66b409fc8fe25de52268e57d1d36c5e723c237de46c9025d"
